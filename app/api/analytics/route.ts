// Phase 2: Analytics dashboard API — scaffold only

import { NextResponse } from "next/server";

export async function GET() {
  return NextResponse.json(
    { error: "Not implemented — Phase 2 feature" },
    { status: 501 }
  );
}
